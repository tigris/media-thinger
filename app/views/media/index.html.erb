<% form_tag(media_path, :method => :get) do %>
  <div><%= text_field_tag :search %></div>
  <%= button_for :find %>
<% end %>

<% unless @entries.blank? %>
<h2>Search results</h2>
<ul>
  <%= render :partial => 'shared/entry', :collection => @entries, :as => :entry %>
</ul>
<% end %>
